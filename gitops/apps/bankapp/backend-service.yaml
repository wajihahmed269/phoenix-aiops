apiVersion: v1
kind: Service
metadata:
  name: banking-backend
  namespace: bankapp
  labels:
    app.kubernetes.io/name: banking-backend
    app.kubernetes.io/part-of: bankapp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: banking-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
