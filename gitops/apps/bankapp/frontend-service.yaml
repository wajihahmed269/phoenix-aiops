apiVersion: v1
kind: Service
metadata:
  name: bankapp-frontend
  namespace: bankapp
  labels:
    app.kubernetes.io/name: bankapp-frontend
    app.kubernetes.io/part-of: bankapp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bankapp-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
